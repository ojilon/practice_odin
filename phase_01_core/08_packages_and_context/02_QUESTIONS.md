# Questions — Packages & Context (Capstone)

1. Draw your package graph (boxes + arrows). Why does it point that way, and where would a cycle have appeared if you had merged two responsibilities?
   > Your answer:

2. List your full public surface. For each public symbol, who calls it and why couldn't it be private?
   > Your answer:

3. Context drill results: what did heap vs arena vs temp runs each require for teardown, and what did callee code *not* need to change? When is explicit `ctx` passing better than implicit inheritance?
   > Your answer:

4. Reuse ledger: which idea in this capstone came from each of folders 02–07? (One line per folder.)
   > Your answer:

5. Tracking reports for happy + error paths: summarize. What is still fragile or ugly, and what will you harden in Phase 2?
   > Your answer:

6. Sign-off (copy to PROGRESS.md when done):
   > `DONE 08_packages_and_context — tracking clean, demo <name> runs, reuse: <packages>`
