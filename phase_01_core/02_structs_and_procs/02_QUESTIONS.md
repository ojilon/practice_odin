# Questions — Structs & Procs

1. When do you pass a struct by value vs by pointer (`^T`)? Give one example of each from your demo.
   > Your answer:

2. What did `distinct` catch (or what would it catch) in your code? Show the line that would fail to compile with a raw `int`.
   > Your answer:

3. Where did you use overloading vs a parametric proc, and why was each the right tool there?
   > Your answer:

4. KATA reflection: which part of this folder could you now rewrite from a blank file in under 20 minutes, and which part still needs the reference open?
   > Your answer:
