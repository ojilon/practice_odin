# Questions — Slices & Arrays

1. What is the difference between a slice (`[]T`) and a dynamic array (`[dynamic]T`)? Who frees each?
   > Your answer:

2. What happened in your dangling-trap version, and which fix did you choose (caller buffer / owned clone / arena)? Why that one?
   > Your answer:

3. What did `len`/`cap` print across make → grow → clear → delete, and what does `clear` preserve that `delete` does not?
   > Your answer:

4. Paste or summarize your tracking-allocator report for the fixed demo. What does silence prove, and what does it *not* prove?
   > Your answer:

5. (If attempted) What did the 100k-append benchmark show, and what build flags + machine did you run it on?
   > Your answer:
