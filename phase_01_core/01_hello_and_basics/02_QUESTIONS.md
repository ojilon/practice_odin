# Questions — Hello and Basics

1. What is the difference between a `string` and a `cstring` in Odin?
   > Your answer:

2. Where does the `fmt` package come from, and how do you import it?
   > Your answer:

3. Why does every `.odin` file in a directory need the same `package` declaration?
   > Your answer:
