package main

import "core:fmt"

// Scratch entry point — change imports and calls as you move between exercise packages.
// Reviewers ignore this file.

main :: proc() {
    fmt.println("practice_odin scratch runner")
    fmt.println("Import the package you are currently practicing and call its demo procedures here.")
}
