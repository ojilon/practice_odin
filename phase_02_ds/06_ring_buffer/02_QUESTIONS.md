# Questions — Ring Buffer

1. What are your index invariants (what do head/count mean, where does wrap happen)? Show the single wrap helper and why all ops use it.
   > Your answer:

2. Strict vs overwrite: tallies from your overflow demo? Which downstream system (input events, log tail, audio window) takes which policy, and why?
   > Your answer:

3. Off-by-one matrix results — which case failed first, and what was the fix?
   > Your answer:

4. Ring vs 03-queue vs 01-buffer: which bounded problem takes which structure? Give one example each.
   > Your answer:

5. (If tried) Modulo vs mask measurement: numbers + verdict on your machine/flags?
   > Your answer:
