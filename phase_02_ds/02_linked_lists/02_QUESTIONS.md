# Questions — Linked Lists

1. What are the five removal cases, and what breaks in pointer surgery if you miss one? Which case bit you?
   > Your answer:

2. Heap-mode vs pool-mode churn numbers (allocs, reuses, time)? When does pooling win, and what does it cost in complexity?
   > Your answer:

3. List vs buffer shootout: numbers + verdict. Which access pattern belongs to which structure?
   > Your answer:

4. What is the handle rule after `remove_node`, and what did the broken trap demonstrate?
   > Your answer:

5. What does the builtin / `core:container` list (if any) do better, and what did building your own teach you that reading wouldn't?
   > Your answer:
