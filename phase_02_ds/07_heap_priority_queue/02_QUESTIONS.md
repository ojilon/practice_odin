# Questions — Heap & Priority Queue

1. Write your parent/child index formulas. Why is `(i-1)/2` correct for a 0-based heap, and what failed when you got one index wrong?
   > Your answer:

2. Sort-proof outputs (min, max, duplicates, edge orders)? What would silent corruption have looked like in the pop-all sequence?
   > Your answer:

3. What is your tie-break rule for equal priorities, and where is it enforced (comparator, seq, or drain)? Show the tied-task output.
   > Your answer:

4. Applied build results (timers / frontier / top-N)? Where will you reuse this heap in Phase 3 (A*) or Phase 7 (scheduler)?
   > Your answer:

5. Heap vs full sort for repeated-min: when does each win (state the complexity argument + any measurement)?
   > Your answer:
